<div class="container">
    <!-- Hero Section -->
    <div class="hero-section text-center mb-5" style="background-color: #082465; color: white; padding: 50px 0;">
        <h1>{{ __('messages.welcome_to_platform') }}</h1>
        <p class="lead">{{ __('messages.journey_begins') }}</p>
    </div>

    <!-- Features Section -->
    <div class="row text-center">
        <div class="col-md-4 mb-4">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">{{ __('messages.user_friendly_interface') }}</h5>
                    <p class="card-text">{{ __('messages.navigate_with_ease') }}</p>
                </div>
            </div>
        </div>
        <div class="col-md-4 mb-4">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">{{ __('messages.support') }}</h5>
                    <p class="card-text">{{ __('messages.support_team_assistance') }}</p>
                </div>
            </div>
        </div>
        <div class="col-md-4 mb-4">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">{{ __('messages.safe_and_secure') }}</h5>
                    <p class="card-text">{{ __('messages.peace_of_mind') }}</p>
                </div>
            </div>
        </div>
    </div>
</div>
